/**
 * ECMAScript 的内置对象
 * 
 * Boolean、Error、Date、RegExp
 */

 let b: Boolean = new Boolean();
 let e: Error = new Error('error');
 let d: Date = new Date();
 let r: RegExp = /[a-z]/;


 /**
  * DOM 和 BOM 的内置对象
  * 
  * Document、HTMLElement、Event、NodeList
  */


/**
 * TypeScript核心库的定义文件
 * 定义了所有浏览器环境需要用到的类型，并且是预置在TypeScript中的
 */